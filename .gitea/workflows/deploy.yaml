name: Build and Deploy

on:
  push:
    branches: [master]

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    container:
      image: oven/bun:latest
      volumes:
        - /var/www/clocks:/deploy
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build

      - name: Deploy
        run: |
          rm -rf /deploy/*
          cp -r dist/* /deploy/
